import './bootstrap';

function addCart(itemId){
    var itemCart = document.createElement()
    var cart = document.getElementById('cart')

    cart.appendChild()

    itemCart.innerHTML = $.get("demo_test.asp", function(data, status){
        alert("Data: " + data + "\nStatus: " + status);
      });
    
      var listItem = new Map([
        ['id', []],
        ['list', []],
    ]);

      listItem.set('id', )

      var listItem = {
        'id':[],
        'list':[]
    }
}